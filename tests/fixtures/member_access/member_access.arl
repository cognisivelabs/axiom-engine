// Member Access Test
// Context: user (object)

let userName: string = user.name;
let userCity: string = user.address.city;

// Return city checks
userCity;

// Deep nesting
let deepZip: string = user.company.address.zip;
userCity + "-" + deepZip

